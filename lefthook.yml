pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.ts"
      run: npm run lint
    format:
      glob: "*.ts"
      run: npm run format

pre-push:
  commands:
    build:
      run: npm run build
